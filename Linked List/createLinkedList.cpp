#include<iostream>
#include<linkedList.h>
using namespace std;
int main()
{
    // Node *temp= new Node(1);
    // add(&temp,2);
    // addRear(&temp,3);
    // display(temp);
   Node *head= makeList(5);
    display(head);
}
